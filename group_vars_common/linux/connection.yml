ansible_connection: ssh
ansible_port: 22
ansible_user: "{{ linux_ansible_user }}"
ansible_ssh_private_key_file: "{{ linux_ssh_private_key_file }}"
ansible_become: "{{ linux_become | default(true) }}"
ansible_become_user: "{{ linux_become_user | default('root') }}"
